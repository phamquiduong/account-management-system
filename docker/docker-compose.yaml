services:
  django:
    build:
      context: ..
      dockerfile: docker/django/Dockerfile
    expose:
      - 8000
    environment:
      - DB_DEFAULT_HOST=postgresql
      - DB_DEFAULT_PORT=5432
      - DB_DEFAULT_USER=${POSTGRES_USER}
      - DB_DEFAULT_PASS=${POSTGRES_PASS}
      - DB_DEFAULT_NAME=${POSTGRES_NAME}

      - EMAIL_HOST=${EMAIL_HOST}
      - EMAIL_PORT=${EMAIL_PORT}
      - EMAIL_HOST_USER=${EMAIL_HOST_USER}
      - EMAIL_HOST_PASSWORD=${EMAIL_HOST_PASSWORD}
      - FROM_EMAIL=${FROM_EMAIL}
    volumes:
      - ../src:/src
      - ../static:/static
      - ../requirements.dev.txt:/requirements.dev.txt
    command: bash -c "python manage.py wait_for_db && python manage.py migrate && \
                      find /static -mindepth 1 -delete && python manage.py collectstatic --noinput && \
                      python manage.py runserver 0.0.0.0:8000"
    restart: always

  postgresql:
    image: postgres:latest
    ports:
      - ${POSTGRES_PORT}:5432
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASS}
      POSTGRES_DB: ${POSTGRES_NAME}
    volumes:
      - ./postgresql/data:/var/lib/postgresql
    restart: always

  nginx:
    image: nginx:latest
    ports:
      - ${NGINX_PORT}:80
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ../static:/app/static
    restart: always
